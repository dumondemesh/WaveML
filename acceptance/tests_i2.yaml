# F2 / P2 — Swaps & L_struct acceptance
schema: 1
tests:
  - id: i2_allow_commute_wt
    kind: allowed
    input: examples/graph/i2_allow_commute_wt.json
    expect:
      delta_l_struct_max: 0.0
      nf_id_unchanged: true

  - id: i2_allow_w_w_same_params
    kind: allowed
    input: examples/graph/i2_allow_w_w_same.json
    expect:
      delta_l_struct_max: 0.0
      nf_id_unchanged: true

  - id: i2_forbid_a_align
    kind: forbidden
    input: examples/graph/i2_forbid_a_align.json
    expect:
      must_fail_linter: true

  - id: i2_forbid_d_without_aa
    kind: forbidden
    input: examples/graph/i2_forbid_d_wo_aa.json
    expect:
      must_fail_linter: true
